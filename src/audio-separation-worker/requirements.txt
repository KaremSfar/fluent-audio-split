kombu>=5.3
onnxruntime
# torch and torchaudio are installed separately in the Dockerfile
# from the official PyTorch CPU index to avoid timeout on the default PyPI CDN.
# Use audio-separator without extras since torch is already present.
audio-separator
numpy
soundfile
